runpod>=1.7.6,<2.0.0
# Was numpy<2.0.0 — that pin predates this pin's own comment history and
# appears to have been written defensively for mediapipe, but 0.10.14
# (pinned below) is confirmed compatible with numpy 2.x; 0.10.15+ is what
# actually needs numpy<2. Constraining numpy<2 forced pip toward an old
# scipy built for the same ABI, which is what caused two different import
# failures in a row (a numpy.ufunc mismatch, then a RecursionError deep in
# diffusers' scheduler) — both symptoms of a numpy/scipy ABI pairing this
# codebase was never actually pinned to run on. Letting both resolve
# together, unconstrained, is the fix, not another guessed pin.
numpy>=1.26.4
mediapipe==0.10.14
Pillow>=10.4.0
requests>=2.32.3
accelerate>=0.34.2
safetensors>=0.4.5
sentencepiece>=0.2.0
transformers>=4.55.0
huggingface_hub[hf_transfer]>=0.26.2
pyfacer>=0.0.5
# 0.40.0's QwenEmbedRope.forward() signature is (video_fhw, device=None,
# max_txt_seq_len=None) — this repo's vendored pipeline_qwenimage_edit_plus.py
# calls pos_embed(img_shapes, txt_seq_lens, device=...), written against the
# OLDER (video_fhw, txt_seq_lens, device) signature: txt_seq_lens lands
# positionally on 0.40.0's `device` parameter, and the explicit device=
# keyword then collides with it — "got multiple values for argument
# 'device'". Confirmed against diffusers' own source at both tags: 0.35.0
# still has the 3-positional-arg signature this pipeline expects; 0.40.0
# does not. Pinned to the last version confirmed compatible with the
# vendored call site, not the newest tagged release.
diffusers==0.35.0
