runpod>=1.7.6,<2.0.0
numpy>=1.26.4,<2.0.0
# Pinned explicitly: left unpinned, pip resolves a scipy built against
# numpy 2.x (its scipy.special ufuncs stopped satisfying numpy 1.x's
# PyUFuncObject check — "All ufuncs must have type numpy.ufunc" at import).
# scipy 1.13.x is the last line built for numpy<2.
scipy>=1.11,<1.14
mediapipe==0.10.14
Pillow>=10.4.0
requests>=2.32.3
accelerate>=0.34.2
safetensors>=0.4.5
sentencepiece>=0.2.0
transformers>=4.55.0
huggingface_hub[hf_transfer]>=0.26.2
pyfacer>=0.0.5
# Was git+https://github.com/huggingface/diffusers.git (the moving dev HEAD,
# built here as 0.40.0.dev0) — switched to the tagged 0.40.0 release after a
# RecursionError deep in its scheduler's scipy import (unreproduced by any
# fix on this end) that a moving, untagged HEAD gives no way to bisect.
diffusers==0.40.0
